import 'package:flutter/widgets.dart';

import 'login_view.dart';

class RegistrationView extends StatelessWidget {
  const RegistrationView({super.key});

  @override
  Widget build(BuildContext context) => const LoginView(initialPage: 1);
}
